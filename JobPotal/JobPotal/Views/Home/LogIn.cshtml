<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Centered Login Form</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background-color: #f8f9fa;
        }

        .card-body {
            width: 500px; /* Increased width */
            height: 350px;
            max-width: 100%;
            padding: 2.5rem; /* Increased padding */
        }

        .card {
            border-start-start-radius: 40px;
            border-end-end-radius: 40px;
            box-shadow: 10px 10px 20px rgba(0, 0, 0, 0.3);
        }

        .logo img {
            display: block;
            margin: 0 auto;
            max-width: 165px;
        }
    </style>
</head>
<body>
    <div class="logo mb-4">
        <a href="index.html"><img src="/assets/img/logo/logo.png" alt="Company Logo"></a>
    </div>
    <div class="card">
        <div class="card-body">
            <form action="/AdminLogin" method="post">
                <div class="mb-3">
                    <label for="email" class="form-label">Email address</label>
                    <input type="email" class="form-control" id="email" name="UserName" placeholder="Enter your email" aria-label="Email Address" required>
                </div>
                <div class="mb-3">
                    <label for="password" class="form-label">Password</label>
                    <input type="password" class="form-control" id="password" name="Password" placeholder="Enter your password" aria-label="Password" required>
                </div>
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <input type="checkbox" id="rememberMe" name="RememberMe">
                        <label for="rememberMe">Remember me</label>
                    </div>
                    <a href="/forgot-password.html" class="text-decoration-none">Forgot Password?</a>
                </div>
                <div class="mt-3">
                    <button type="submit" class="btn btn-primary w-100">Login</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>